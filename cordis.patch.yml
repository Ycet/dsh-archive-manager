# dsh-archive-manager — 设置页「归档」插件（查看/筛选/排序/取消归档/删除归档会话）。
# 声明 dsh.bundle 补丁层：`dsh plugin --profile web add <pkg>` 安装时自动合入
# profile 的 bundle 层，无需手动编辑 cordis.patch.yml。
- insert:
    - id: archive-manager
      name: dsh-archive-manager
